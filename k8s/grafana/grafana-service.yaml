---
apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: session-database
  labels:
    app: grafana
spec:
  selector:
    app: grafana
    component: monitoring
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
